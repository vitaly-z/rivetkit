{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["src/**", "./tsup.config.ts"],
      "dependsOn": [
        "@actor-core/common#build",
        "@actor-core/actor-protocol#build"
      ]
    }
  }
}
