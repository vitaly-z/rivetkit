{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "test": {
      "dependsOn": ["^build", "check-types", "build"],
      "env": [
        "RIVET_ENDPOINT",
        "RIVET_CLOUD_TOKEN",
        "RIVET_PROJECT",
        "RIVET_ENVIRONMENT",
        "_RIVET_SKIP_DEPLOY"
      ]
    }
  }
}
