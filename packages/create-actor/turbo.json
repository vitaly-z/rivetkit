{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["src/**", "./tsup.config.ts"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "inputs": ["src/**", "./tsup.config.ts"],
      "outputs": ["dist/**"]
    }
  }
}
